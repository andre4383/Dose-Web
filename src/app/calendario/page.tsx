import { CalendarView } from '@/components/calendar-view';

export default function CalendarioPage() {
  return (
    <main className="mx-auto w-full max-w-md sm:max-w-2xl px-5 sm:px-6 py-8 sm:py-12">
      <CalendarView />
    </main>
  );
}
